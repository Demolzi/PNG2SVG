# 批量闭合区域抠图并导出 SVG 优化方案

## 1. 产品定位

开发一个本地桌面软件，用于批量导入图片，手动绘制多个闭合区域，并将每个区域单独抠出为透明背景结果，最终导出为独立 SVG 文件。

核心定位：

> 批量图片闭合区域抠图工具：用户手动画闭合区域，软件按区域生成透明 PNG，并以内嵌 PNG 的方式导出为 SVG；每个闭合区域导出一个独立 SVG 文件。

本方案基于以下确认结果：

1. SVG 接受“内嵌透明 PNG”的形式。
2. 第一版只做基础闭合区域抠图。
3. 第一版不需要保存项目文件。
4. 每个区域单独导出一个 SVG 文件。

---

## 2. 第一版目标

第一版重点实现：

1. 批量导入图片。
2. 在每张图片上绘制多个闭合区域。
3. 检查区域是否合法，避免自交、交叉、重叠和包含关系。
4. 根据闭合区域生成透明抠图结果。
5. 每个区域导出一个独立 SVG 文件。
6. SVG 内部以内嵌 base64 PNG 的方式保存图像内容。

第一版不实现：

1. 真正矢量化 SVG。
2. AI 智能抠图。
3. 项目文件保存和再次打开。
4. 复杂控制点编辑。
5. 多区域合并到一个 SVG。

---

## 3. 技术选型

建议使用以下技术栈：

| 技术 | 用途 |
|---|---|
| Python | 主开发语言 |
| PySide6 | 桌面 GUI、画布、工具栏、图片列表 |
| OpenCV / Pillow | 图片读取、裁剪、透明通道、蒙版、羽化 |
| Shapely | 闭合区域合法性检查 |
| svgwrite | 生成 SVG 文件 |
| PyInstaller | 后期打包 Windows `.exe` |

第一版不建议使用：

- `rembg`
- `onnxruntime`
- OpenCV GrabCut
- 图像矢量化工具

原因是第一版已经确认只做基础闭合区域抠图，不需要智能抠图或真矢量化。

---

## 4. 功能范围

### 4.1 图片导入

支持：

- 单张图片导入
- 多张图片批量导入
- 文件夹导入

支持格式：

- `.png`
- `.jpg`
- `.jpeg`

左侧图片列表显示：

- 文件名
- 已绘制区域数量
- 是否存在非法区域
- 是否已导出

---

### 4.2 画布预览

画布支持：

- 图片居中显示
- 鼠标滚轮缩放
- 按住空格或鼠标中键拖动画布
- 当前图片区域覆盖显示
- 当前选中区域高亮显示
- 非法区域用红色显示

第一版不需要复杂图层管理，只需要支持当前图片的区域绘制、选择和显示。

---

### 4.3 闭合区域绘制

第一版建议支持三类区域：

1. 矩形
2. 椭圆
3. 自由闭合曲线

#### 4.3.1 矩形工具

用户拖拽生成矩形区域。

内部数据示例：

```json
{
  "type": "rect",
  "x": 120,
  "y": 80,
  "width": 300,
  "height": 200
}
```

矩形在几何校验和导出时转换为多边形。

#### 4.3.2 椭圆工具

用户拖拽生成椭圆区域。

内部数据示例：

```json
{
  "type": "ellipse",
  "cx": 300,
  "cy": 200,
  "rx": 120,
  "ry": 80
}
```

椭圆在导出和校验时转换为多边形。

建议采样精度：

| 场景 | 点数 |
|---|---:|
| 预览 | 64 |
| 默认导出 | 128 |
| 大图或高精度导出 | 256 |

#### 4.3.3 自由闭合曲线工具

用户按住鼠标绘制，鼠标松开后自动闭合。

内部数据示例：

```json
{
  "type": "freehand",
  "points": [
    [120, 80],
    [150, 95],
    [170, 130]
  ]
}
```

自由曲线建议处理：

- 鼠标移动距离小于 2 到 4 像素时不记录点。
- 绘制结束后做一次曲线简化。
- 点数太少时拒绝保存，例如少于 3 个有效点。
- 面积太小时拒绝保存。

---

## 5. 区域合法性检查

每次新增区域后立即检查。

### 5.1 自身合法性

拒绝以下情况：

- 区域点数不足
- 多边形面积过小
- 自由曲线自交
- 多边形无效
- 区域超出图片边界过多

建议使用 Shapely：

```python
polygon.is_valid
polygon.area
```

---

### 5.2 区域之间的合法性

第一版建议采用严格规则，降低归属歧义。

允许：

- 区域完全分离

不允许：

- 区域边界交叉
- 区域重叠
- 区域边界重合
- 一个区域完全包含另一个区域

判断建议：

```python
new_polygon.crosses(old_polygon)
new_polygon.overlaps(old_polygon)
new_polygon.boundary.intersects(old_polygon.boundary)
new_polygon.contains(old_polygon)
old_polygon.contains(new_polygon)
```

不建议直接只用 `intersects` 做最终判断，因为它会把边界接触、包含、重叠等多种情况混在一起，不利于给用户明确提示。

---

### 5.3 错误提示

非法区域不保存，并提示原因：

- “当前曲线自交，请重新绘制。”
- “当前区域与已有区域重叠。”
- “当前区域与已有区域边界交叉。”
- “当前区域包含已有区域，第一版暂不支持包含关系。”
- “当前区域面积过小。”

---

## 6. 抠图处理逻辑

第一版只做基础闭合区域蒙版抠图。

### 6.1 单区域处理流程

对每个区域：

1. 获取区域多边形。
2. 计算区域外接矩形 bounding box。
3. 从原图中裁剪 ROI。
4. 在 ROI 内生成 alpha 蒙版。
5. 蒙版内部不透明。
6. 蒙版外部透明。
7. 边缘做轻微羽化。
8. 得到透明 PNG。
9. 将透明 PNG 写入 SVG。

---

### 6.2 导出尺寸策略

建议采用：

> 每个区域按自身外接矩形裁剪后导出。

也就是每个 SVG 只包含该区域附近的最小图像范围，而不是整张原图大小。

优点：

- 文件更小。
- 每个导出的 SVG 更像独立素材。
- 后续插入其他软件更方便。
- 避免大面积透明空白。

SVG 尺寸设置示例：

```xml
<svg width="区域宽度" height="区域高度" viewBox="0 0 区域宽度 区域高度">
  <image href="data:image/png;base64,..." width="区域宽度" height="区域高度"/>
</svg>
```

---

## 7. SVG 导出规则

### 7.1 导出形式

每个闭合区域导出一个独立 SVG 文件。

SVG 内容包括：

- 一个 `<svg>` 根节点
- 一个内嵌 base64 PNG 的 `<image>` 节点
- PNG 带透明 alpha 通道

不做真正矢量路径转换。

---

### 7.2 文件命名规则

默认命名：

```text
{原图文件名}_{序号}_{区域名}.svg
```

示例：

```text
motor_001_rotor.svg
motor_002_region002.svg
board_001_chip.svg
```

---

### 7.3 文件名安全处理

导出前需要处理非法字符。

Windows 文件名中禁止：

```text
\ / : * ? " < > |
```

处理规则：

- 非法字符替换为 `_`。
- 区域名为空时使用 `region001`。
- 文件重名时自动追加 `_copy1`、`_copy2`。
- 原图扩展名不参与导出文件名。
- 中文区域名允许保留。

---

### 7.4 导出目录结构

建议输出结构：

```text
output/
  motor/
    motor_001_region001.svg
    motor_002_region002.svg
  board/
    board_001_region001.svg
```

每张原图一个子文件夹，避免不同图片之间文件名冲突。

---

## 8. 软件界面设计

主界面建议：

```text
+----------------------------------------------------------------+
| 导入图片 | 导入文件夹 | 矩形 | 椭圆 | 自由曲线 | 删除区域 | 导出 |
+----------------+--------------------------------+--------------+
| 图片列表       |            图片画布             | 区域列表     |
|                |                                | 区域名称     |
| motor.png      |                                | 导出目录     |
| board.jpg      |                                | 羽化半径     |
|                |                                | 导出按钮     |
+----------------+--------------------------------+--------------+
| 状态栏：当前图片、缩放比例、区域数量、校验结果、导出进度       |
+----------------------------------------------------------------+
```

---

### 8.1 右侧区域列表

每个区域显示：

- 序号
- 名称
- 类型：矩形 / 椭圆 / 自由曲线
- 状态：合法 / 非法
- 显示 / 隐藏
- 删除按钮

第一版建议支持：

- 区域重命名
- 区域删除
- 区域选中
- 区域隐藏 / 显示

第一版不建议支持：

- 复杂控制点编辑
- 区域复制
- 图层顺序
- 布尔运算

---

## 9. 内部数据结构

由于第一版不需要保存项目文件，可以只在内存中维护数据。

建议内存结构：

```python
class ImageItem:
    path: str
    width: int
    height: int
    regions: list[Region]

class Region:
    id: str
    name: str
    shape_type: str
    data: dict
    polygon_points: list[tuple[float, float]]
    is_valid: bool
    error_message: str | None
```

示例数据：

```json
{
  "path": "D:/images/motor.png",
  "regions": [
    {
      "id": "region001",
      "name": "rotor",
      "shape_type": "freehand",
      "points": [[120, 80], [180, 90], [200, 150]],
      "is_valid": true
    }
  ]
}
```

虽然第一版不保存项目文件，但建议代码结构保留扩展空间，以后如果需要保存项目，可以再加 `.cutproj.json`。

---

## 10. 推荐代码结构

```text
batch_cutout_svg/
  app.py
  requirements.txt

  ui/
    main_window.py
    canvas.py
    image_list.py
    region_panel.py

  core/
    image_loader.py
    geometry.py
    mask.py
    exporter.py
    naming.py

  models/
    image_item.py
    region.py

  tests/
    test_geometry.py
    test_mask.py
    test_naming.py
    test_exporter.py
```

模块职责：

| 文件 | 职责 |
|---|---|
| `app.py` | 程序入口 |
| `main_window.py` | 主窗口布局 |
| `canvas.py` | 图片显示、缩放、拖动、绘制区域 |
| `image_list.py` | 左侧图片列表 |
| `region_panel.py` | 右侧区域列表和导出设置 |
| `image_loader.py` | 图片读取和格式检查 |
| `geometry.py` | 多边形转换、合法性检查 |
| `mask.py` | 生成透明 PNG、边缘羽化 |
| `exporter.py` | SVG 导出 |
| `naming.py` | 文件名清洗和冲突处理 |
| `image_item.py` | 图片数据模型 |
| `region.py` | 区域数据模型 |

---

## 11. MVP 实施阶段

### 阶段 1：基础闭环

目标：能完成单图单区域导出。

实现：

- PySide6 主窗口
- 单张图片导入
- 图片画布显示
- 矩形区域绘制
- 区域蒙版生成
- 单区域导出 SVG

验收标准：

- 能打开一张图片。
- 能画一个矩形区域。
- 能导出一个透明背景 SVG。
- SVG 打开后只显示矩形区域内图像。

---

### 阶段 2：多区域和批量图片

目标：支持多图、多区域。

实现：

- 多图片导入
- 文件夹导入
- 图片列表切换
- 每张图片多个区域
- 区域列表显示
- 区域重命名和删除
- 每个区域单独导出 SVG

验收标准：

- 多张图片可以分别绘制区域。
- 每张图片可以有多个区域。
- 导出后每个区域生成一个 SVG。
- 文件名符合命名规则。

---

### 阶段 3：椭圆和自由曲线

目标：完善绘制工具。

实现：

- 椭圆工具
- 自由闭合曲线工具
- 曲线点位过滤
- 曲线简化
- 自动闭合
- 区域半透明覆盖显示

验收标准：

- 能画椭圆区域并导出。
- 能画自由曲线区域并导出。
- 自由曲线边缘基本平滑。
- 区域外透明。

---

### 阶段 4：合法性检查

目标：避免区域冲突。

实现：

- 区域自交检测
- 区域间重叠检测
- 区域交叉检测
- 包含关系拒绝
- 非法原因提示

验收标准：

- 自交曲线被拒绝。
- 重叠区域被拒绝。
- 交叉区域被拒绝。
- 包含区域被拒绝。
- 错误提示清晰。

---

### 阶段 5：导出体验完善

目标：提高可用性。

实现：

- 导出目录选择
- 批量导出进度条
- 导出成功数量显示
- 导出失败列表
- 文件名非法字符处理
- 同名文件自动避让
- 羽化半径设置

验收标准：

- 可以一键导出全部合法区域。
- 导出失败时能看到原因。
- 文件不会被意外覆盖。
- 输出目录结构清晰。

---

## 12. 第一版参数建议

| 参数 | 建议默认值 |
|---|---:|
| 自由曲线最小点距 | 3 px |
| 自由曲线简化容差 | 1.5 px |
| 椭圆预览采样点 | 64 |
| 椭圆导出采样点 | 128 |
| 边缘羽化半径 | 1 到 2 px |
| 最小区域面积 | 25 px² |
| 默认导出格式 | SVG 内嵌 PNG |
| 默认导出方式 | 每个区域一个 SVG |
| 默认包含关系 | 不允许 |
| 默认项目保存 | 不支持 |

---

## 13. 主要风险和应对

### 风险 1：用户误以为 SVG 是真矢量

应对：

在界面和说明文档中明确写：

> 当前导出的 SVG 为内嵌透明 PNG 的 SVG 文件，用于保留照片细节和透明背景，不是矢量路径化结果。

---

### 风险 2：自由曲线绘制效果不稳定

应对：

第一版加入点位过滤和曲线简化，不做复杂控制点编辑。画错就删除重画。

---

### 风险 3：区域交叉判断误判

应对：

先采用严格规则。只允许完全分离区域，不允许重叠、交叉、边界重合、包含关系。

---

### 风险 4：导出文件过大

应对：

每个区域只导出外接矩形 ROI，不导出整张图。PNG 可在后续增加压缩选项。

---

### 风险 5：批量导出时部分图片失败

应对：

导出过程不中断。失败项记录到列表中，最后统一提示。

---

## 14. 最终验收标准

第一版完成后，应满足以下标准：

1. 可以导入单张、多张图片或文件夹。
2. 可以在每张图片上绘制矩形、椭圆、自由闭合曲线。
3. 每张图片可以包含多个区域。
4. 非法区域会被拒绝或提示。
5. 每个区域可以命名。
6. 每个区域单独导出一个 SVG。
7. SVG 内嵌透明 PNG。
8. 导出结果背景透明。
9. 文件名自动按规则生成。
10. 批量导出时有进度和失败提示。

---

## 15. 简化后的最终开发目标

最终第一版可以概括为：

> 一个本地批量抠图小工具。用户导入图片，在图上画闭合区域，软件检查区域是否合法，然后把每个区域裁剪成透明 PNG，并封装成独立 SVG 文件导出。

这个范围清晰、技术可控、实现成本合理，适合直接进入开发。
